//it is function declaration and defenation
 function showMessage(message) {
    document.getElementById('message').textContent = message;
}
//it is a single line code which can shown anywhere
//java is casesensitive...
